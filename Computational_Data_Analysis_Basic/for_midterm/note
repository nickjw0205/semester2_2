find => 원소의 첫번째 위치를 반환해줌, 없으면 -1
index => 같은 기능을 하나 이건 없으면 에러가 남

join => 문자열 삽입
	ex)
	>>> a = ","
	>>> a.join("abcd") => a,b,c,d

	>>> a = "abcd"
	>>> b = "ㄱㄴㄷㄹ"
	>>> a.join(b)
	'ㄱabcdㄴabcdㄷabcdㄹ'

	>>> b = "r"
	>>> a.join(b)
	'r'

upper / lower => 대소문자로 바꾸기 => 출력만 되지 값이 바뀌지는 않음

lstrip / rstrip / strip => 왼쪽,오른쪽, 양쪽 공백 지우기


문자열 바꾸기

	>>> a.replace("Life", "Your leg")
	'Your leg is too short'


문자열 나누기(split)

	>>> a = "Life is too short" 
	>>> a.split()
	['Life', 'is', 'too', 'short'] 

	>>> a = "a:b:c:d"
	>>> a.split(':') 
	['a', 'b', 'c', 'd']


append()
리스트 제일 뒤에 항목을 추가한다.
pop()
리스트 제일 뒤의 항목을 빼내고, 빼낸 항목은 삭제한다.
sort()
리스트의 항목을 정렬한다.
reverse()
리스트의 항목의 순서를 역순으로 만든다.
index()
지정한 값을 찾아서 그 위치를 반환한다.
insert()
지정된 위치에 값을 삽입한다.
remove()
리스트에서 지정한 값을 제어한다.
단 지정한 값이 여러 개일 경우 첫 번째 값만 지운다.
extend()
리스트 뒤에 리스트를 추가한다.
count()
리스트에서 찾을 값의 개수를 센다.



tuple => 변경이 불가능하게 만들어야할경우
set => indexing 이나 slicing이 불가능, 중복 불가능
dictionary => key에는 list 사용불가능(변경불가능한 것이어야함)
