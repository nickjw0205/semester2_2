#공지#
보강은 동영상강의로 대체 하겠음.


이름상자로 범위를 지정할수있었죠. 기억해야죠
지우려면 수식 => 정의된이름=>이름관리자=>거기서 삭제!
잘못만들면 삭제를 하면됩니다!간단!

수원 => 수원"점"으로 바꿔보자! 사용자 지정표시 형식으로!

단축키: (ctrl + 1)
숫자는 0또는# , 문자는 @ =>@"점" / @점
(수식이 아니기에 따옴표를 안해도 된다.)

각각의 범위에서 몇번째 값을 구할것이냐! 
large() / small()
몇번째로 큰값이나 작은 값을 구하기 위한 함수
(max() / min()의 기능 가능!)

평균은 갑자기 너무 작은값이나 큰값이 들어오면 정홧ㄱ도가 떨어짐

median()
주어진 수들에 대해 순서적으로 중간에 있는 값을 구함
(평균보다 많이 사용! 결과에 영향을 덜 줘서)
만약 개수가 짝수개여서 중간값이 2개면 그 2개의 평균을 구한다.


최빈값: 가장 많이 나온 값
mode.sngl() => 데이터 집합에서 가장 자주 발생하는 값을 구해주는 함수

숫자들이 적혀잇는곳을 드래그로 영역을 설정하면 엑셀의 워크시트 줄에
평균, 개수, 합계가 뜬다!

true의 값은 1이다 => =true + true => 2

=을 안붙여도 +를 붙이면 계산을 하는거라 인식을합니다!

맨앞에 작은따옴표를 붙이면 계산을 하는게 아니라고 인식 
=> '+연산자 =(출력)> +연산자
=> '=1+2 =(출력)> =1+2

조건부서식 => 특정 텍스트를 포함하는 행
=> 새규칙 => 수식을 사용하여~~
찾는 값을 포함한 열 = (찾는값) 으로 수식을 쓰면됨

countif =>조건을 주고 셀수있는것이죠

조건을 주고 평균을 구하려면 AVERAGEIF!!

true부분에 특정 범위를 넣으면 true인 부분의 값만 리턴해준다.
but 전체에서 특정셀을 찾고싶을때 범위=특정셀 을 하면 에러가 난다!
이때 배열수식을 써여 => 그냥 엔터가 아니라 ctrl + shift + enter
단계적으로 계산하세요 배열에서 하나씩 보면서 같으면 연산하세요
배열수식으로 연산하면 수식이 {}으로 둘러쌓여있음















