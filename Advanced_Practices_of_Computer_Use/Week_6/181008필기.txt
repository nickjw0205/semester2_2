상대참조 : 수식을 복사했을 때, 참조한 셀의 주소가 상대적으로 바뀜.
절대참조 : 수식을 복사했을 때, 참조한 셀의 주소가 바뀌지 않음.
혼합참조 : 상대참조와 절대참조를 같이 사용하여 수식을 복사했을 경우.

소수점 없애기 -> 홈탭 표시형식에서 자리수 줄이기 버튼 누르기

조건부서식: 홈탭 -> 스타일 -> 조건부서식 -> 강조 -> 텍스트포함

발생날짜로는 오늘을 기준으로 하기에 특정 날짜는 불가능하다.(어제 1달전 그런식임)

조건부서식에서 기타 ->같지않다 ->  "<>"의 수식을 씀

상위 5명 -> 상위/하위 규칙 -> 상위10개항목 -> 10개를 5개로 바꾸기

날짜도 숫자로 저장되기에 최근 날짜일수록 값이 크다.

데이터막대: 높은값과 낮은 값을 쉽게 구분하고자 할 때

색조 : 데이터의 흐름을 쉽게 파악하고자 할 때 -> 서서히증가, 서서히 감소, 유지 등을 확인

아이콘 -> 3개짜리로 하면 전체 비율에서 1/3씩 나눠서 해줌

규칙을 편집하거나 삭제할때 -> 규칙관리

ex)부장인 애의 전체 데이터를 바꾸고 싶다. -> 이때는 특정 열만 선택하는게 아니라 데이터 표 전체를 영역으로 지정해준다.

같은지 비교할때 문자를 넣으면 꼭 큰따옴표를 해야함.

