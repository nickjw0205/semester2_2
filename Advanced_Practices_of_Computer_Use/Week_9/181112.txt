저번시간 문자열조작함수=> left(), mid(), right()

사업자등록번호 => 10자리(2자리, 3자리, 5자리로 나눔)

셀서식 => ctrl + 1

숫자로 보여도 함수로 부분추출한 결과는 텍스트로 나옴.

숫자문자를 숫자로 바꾸는 함수 => value => 텍스트형태의 숫자를 실제 숫자로 바꿔줌

표시형식을 주는 함수도 존재함(사용자지정 형식을 통해 하는걸 함수로 할수있다.)
=>text() 함수! 빠바밤! format은 텍스트 이기에 꼭 큰따옴표를 해야한다.

문자열의 전체길이를 아는함수 => len(), 파이썬하고 같네...

수정이 불가하도록 막는 방법 => 
1. 셀서식 => 보호 => 잠금(수정불가), 숨김(식이 안보임)
2. 검토 => 변경내용 => 시트보호

주의! 안잠글 부분은 선택해서 잠금체크 해제시켜야함

& 기호를 쓰면 텍스트를 합칠수 있었다. => 함수로 해보자ㅏㅏㅏㅏㅏㅏㅏㅏㅏㅏ
==> CONCATENATE()

성만 추출할때 => 첫번째 공백의 위치를 알아내 공백위치 - 1 하면 성이 짜란

substitute() => 원본텍스트에서 특정텍스트를 찾아서 다른 텍스트로 바꿔줌

aaaa => *요일, dddd => 영어로  => weekday()함수를 사용하자(숫자로 요일을 알려줌)

choose() 1부터 시작해서 하나의 단어와 매칭을 해놓는 함수



